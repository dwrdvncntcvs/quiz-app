import React from "react";
import scss from "../../styles/noContent.module.scss";

const NoContent = () => {
  return (
    <div className={scss["no-content"]}>Content will be added soon...</div>
  );
};

export default NoContent;
